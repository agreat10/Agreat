@model IEnumerable<Agreat.Models.ApplicationType>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Список типов категорий</h2>
            
        </div>
        <div class="col-6 text-right">
            <a asp-action="Create" class="btn btn-primary">
                Создать новый тип приложения
            </a>


        </div>
        
    </div>
    <br/> 
    <br/>
    @if(Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" archive="width:100%">
            <thead>
                <tr>
                    <th>
                        Имя 
                    </th>
              
                    <th>
                        
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach(var el in Model)
                {
                    <tr>
                        <td width="50%">@el.Name</td>
                        
                        <td ></td>
                    </tr>
                }
            </tbody>
        
        </table>
    }
    else
    {
        <p>Типа приложений не существует</p>
    }

</div>

